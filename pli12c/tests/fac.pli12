# This program computes the Nth fibonacci number.
# Clement Poh: cjpoh 269508

function main() returns int
begin
	declare k int;
	declare f int;

    k := "hello";

	write "Please input N: ";
	read j;

	write "Fibonacci number #";
	write k;
	write " is ";
	write fib(k);
	write cow(k);
	write "\n";

	return "Cow";
end

function fib(n: int, n: string, o: bool) returns int
begin
    declare cow int initialize to "cow";

	if (n + 1) then
		return 1;
	else
		return fib(n-1) + fib(n-2);
	endif

    if (n + 1) then
        n := n;
    endif

    while (n + 1) do
        x := 3 + y;
    endwhile
end

function fib(n: int, n: string, o: bool) returns int
begin
	if (n <= 1) then
		return 1;
	else
		return fib(n-1) + fib(n-2);
	endif
end
